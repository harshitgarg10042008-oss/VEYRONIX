! INCOMPLETE Cisco IOS — no SSH, no telnet, no AAA, no logging
! This should result in UNKNOWN for most controls
version 16.6
hostname SIH-ROUTER-INCOMPLETE
!
interface GigabitEthernet0/0
 ip address 10.0.0.1 255.255.255.0
 no shutdown
!
end
