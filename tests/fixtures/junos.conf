## Junos Fixture
system {
    host-name core-sw1;
    services {
        ssh {
            protocol-version v2;
        }
        telnet;
    }
    authentication-order [ radius password ];
    syslog {
        host 10.0.0.2 {
            any warning;
        }
    }
    ntp {
        server 10.0.0.1;
    }
}
snmp {
    v3;
}
